{
  "name": "Uptae",
  "description": "Uptae is a Slackbot that lets you know what people are up to.",
  "repository": "https://github.com/weejames/uptae",
  "logo": "",
  "success_url": "/",
  "env": {
    "HEROKU_APP_NAME": {
      "required": true
    },
    "BOT_USERNAME": {
      "description": "The name you want Uptae to use when communicating with your Slack channel",
      "required": true,
      "value": "Uptae"
    },
    "BOT_COMMAND_TOKEN": {
      "description": "Slash command token, for the command endpoint",
      "required": true
    },
    "WEBHOOK_URL": {
      "description": "Webhook URL from Slack to allow the bot to post information to your channel",
      "required": true
    },
    "SLACK_TOKEN": {
      "description": "Slack bot RTM API token",
      "required": false
    },
    "NODE_ENV": "production"
  },
  "image": "heroku/nodejs",
  "addons": [
      {
        "plan": "heroku-postgresql",
        "options": {
          "version": "9.5"
        }
      }
    ]
}
